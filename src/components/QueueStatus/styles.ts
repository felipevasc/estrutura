import styled from 'styled-components';

export const StyledQueueStatus = styled.div`
    position: fixed;
    bottom: 20px;
    right: 20px;
    z-index: 1000;

    .ant-badge {
        cursor: pointer;
    }
`;
