## Threat Weaver

```
run -d -p 3000:3000 felipevasc2/threatweaver:tagname
```

### Variáveis de ambiente

Para habilitar o assistente de IA contextual, configure no `.env`:

```
OPENAI_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
```
